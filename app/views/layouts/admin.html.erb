<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>GitHub PR Statistics</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_link_tag 'sb-admin-2.min', media: 'all', 'data-turbolinks-track': 'reload' %>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body id="page-top">
  <div id="wrapper">
    <%= render 'shared/sidebar' %>
    <div id="content-wrapper" class="d-flex flex-column">
      <div id="content">
        <%= render 'shared/topbar' %>
        <div class="container-fluid">
          <%= yield %>
        </div>
      </div>
      <%= render 'shared/footer' %>
    </div>
  </div>
  <%= render 'shared/scroll_to_top' %>
  <%= render 'shared/logout_modal' %>
  
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@rails/ujs@7.0.0/lib/assets/compiled/rails-ujs.js"></script>
  <script>
    $(document).ready(function() {
      // Initialize Rails UJS for method: :delete links
      Rails.start();
      
      // Initialize Bootstrap dropdowns
      $('.dropdown-toggle').dropdown();
      
      // Ensure the entire user dropdown area is clickable
      $('#userDropdown, #userDropdown span, #userDropdown div').on('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        $('#userDropdown').next('.dropdown-menu').toggle();
      });
    });
  </script>
</body>
</html>
